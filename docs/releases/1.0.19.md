# Pulse 1.0.19

- Settings dropdown values are left aligned, with dedicated space for the trailing chevron.
- Window effect and language menus now open below the control, aligned to its left edge instead of the mouse position. Menu width follows the control, and screen edges constrain placement.
- The current selection has a checkmark; menu labels keep their shared left alignment.

Validation: focused live dropdown checks passed for dark 100% and narrow light 125% layouts. Verified hit targets, actual popup left/bottom coordinates, minimum width, and keyboard selection for both controls. Inspected closed English/Chinese dropdowns and open menu screenshots. No full legacy test suite was run.
