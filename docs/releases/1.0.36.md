- 侧边栏移除「保存的搜索」分区，分区菜单里也不再列出；旧会话里的分区顺序与显隐状态会自动迁移，位置不变。
- 「自动检查更新」开关只负责自动检查与提醒：关闭后不再后台检查、不弹安装提示，「关于与诊断」里的手动「检查更新」与「下载并安装」照常可用。
- 修复新增字符串超出字符串表范围导致的空白文字：「自动检查更新」开关的标题与描述、以及「添加排除文件夹 / 添加服务器目录 / 移除排除文件夹 / 移除服务器目录」四个按钮此前显示为空白。
- 设置页整理：通用页改为外观 / 启动与关闭 / 文件列表三张卡；关于与诊断页去掉重复的状态文字与重复版本号；搜索与索引页的索引状态只保留一处，磁盘列表与排除项列表补上卡片背景。
- 两个「移除」按钮分别命名为「移除排除文件夹」「移除服务器目录」；重复文件页在滚动后的行命中与绘制改用同一份布局数据。
- 本仓库发布的是 dev 频道：安装包名为 `PulseSetup-1.0.36-dev.exe`（Windows 8.1 兼容版为 `PulseSetup-1.0.36-dev-win81.exe`），界面显示的版本带 `-dev` 后缀，更新源指向本仓库发布页并由本仓库自己的密钥签名。dev 包与稳定版共用同一套安装身份，安装会覆盖稳定版并保留现有设置与会话。

继续提供 Windows 10/11 x64 和 Windows 8.1 x64 两种安装包（均属 dev 频道，安装包名带 `-dev` 后缀）。
